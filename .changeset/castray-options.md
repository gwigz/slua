---
"@gwigz/slua-tstl-plugin": minor
"@gwigz/slua-types": minor
---

BREAKING: replace `castRay` and `httpRequest` fluent builders with options-object overloads that compile to flat parameter lists at build time, enabling use as expressions
