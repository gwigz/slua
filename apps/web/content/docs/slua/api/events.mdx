---
title: Events & Timers
description: LLEvents, LLTimers, DetectedEvent, and the event system
---

## LLEvents

The event registration singleton. Use `LLEvents.on()` to subscribe to
Second Life events like `touch_start`, `listen`, `changed`, etc.

<auto-type-table path="../../../../lib/slua-types.d.ts" name="LLEvents" />

## LLTimers

Timer scheduling interface with `every()` and `once()` methods.

<auto-type-table path="../../../../lib/slua-types.d.ts" name="LLTimers" />

## DetectedEvent

Detection info for the object or avatar that triggered a touch, collision, or
sensor event. Has `getKey()`, `getName()`, `getPos()`, and so on.

<auto-type-table path="../../../../lib/slua-types.d.ts" name="DetectedEvent" />

## LLEventMap

The full mapping of event names to their handler signatures.

<auto-type-table path="../../../../lib/slua-types.d.ts" name="LLEventMap" />
