// Stub for typescript-to-lua/dist/transpilation/resolve, same as the official TSTL playground.
export function resolveDependencies(_: unknown, files: unknown[]) {
  return { resolvedFiles: files, diagnostics: [] }
}
