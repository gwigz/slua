export const NOTECARD_NAME = "settings.yml"

/** Note, these are defaults, use the `settings.yml` notecard to override them */
export const config = {
  DIALOG_CHANNEL: -1731704570,
  RATE_LIMIT: 5,
  PENDING_TIMEOUT: 30,
}
